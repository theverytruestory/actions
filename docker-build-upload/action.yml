name: "Docker build and upload to registry"
description: "This will build docker image and upload it to registry"

inputs:
  docker_file:
    description: "Path to dockerfile"
    required: false
    default: "./Dockerfile"
  context:
    description: "Docker context"
    required: false
    default: "."
  registry:
    description: "Docker registry"
    required: false
    default: "registry.htz.tsof.news:5000"
  tag:
    description: "Image tag, if not set it will be generated from branch name and commit hash"
    required: false
    default: ""
  repo:
    description: "Repository name, if not set it will be taken from github event"
    required: false
    default: ""


outputs:
  REPO:
    description: "Image url"
    value: ${{ steps.output.outputs.REPO }}
  TAG:
    description: "Image tag"
    value: ${{ steps.output.outputs.TAG }}

runs:
  using: "composite"
  steps:

    - name: Set vars
      shell: bash
      env:
        BRANCH: ${{ github.head_ref || github.ref_name }}
      run: |
        if [ "${{ inputs.tag }}" != "" ]; then
          TAG="${{ inputs.tag }}"
        else
          TAG="0.$(git rev-list --all --count)-${BRANCH#refs/heads/}-$(git rev-parse --short HEAD)"
        fi
        if [ "${{ inputs.repo }}" != "" ]; then
          REPO="${{ inputs.repo }}"
        else
          REPO="${REGISTRY}/${{ github.event.repository.name }}"
        fi
        echo "TAG="${TAG}"" >> $GITHUB_ENV
        echo "REPO=${REGISTRY}/${{ github.event.repository.name }}" >> $GITHUB_ENV

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        buildkitd-config-inline: |
          [registry."${{ inputs.registry }}"]
            ca=["/usr/local/share/ca-certificates/ttf-root-ca.crt"]

    - name: Build and push
      uses: docker/build-push-action@v6
      env:
        DOCKER_BUILD_RECORD_UPLOAD: false
      with:
        push: true
        context: ${{ inputs.context }}
        file: ${{ inputs.docker_file }}
        tags: ${{ env.REPO }}:${{ env.TAG }}
        cache-from: type=registry,ref=${{ env.REPO }}:buildcache
        cache-to: type=registry,ref=${{ env.REPO }}:buildcache,mode=max

    - name: set outputs
      id: output
      env:
        REPO: ${{ env.REPO }}
        TAG: ${{ env.TAG }}
      shell: bash
      run: |
        echo "REPO=$REPO" >> $GITHUB_OUTPUT
        echo "TAG=$TAG" >> $GITHUB_OUTPUT
        echo -ne '```yaml\nrepo: ${{ env.REPO }}\ntag: ${{ env.TAG }}\n```' | tee -a $GITHUB_STEP_SUMMARY