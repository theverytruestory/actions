name: "ArgoCD set image and commit"
description: "Test image via ArgoCD kustomize-image parameter, then commit to Git if stable"

inputs:
  registry:
    description: "Docker registry"
    required: false
    default: "registry.truestory.work"
  repo:
    description: "Repository name (without registry), if not set will use github.event.repository.name"
    required: false
    default: ""
  tag:
    description: "Docker image tag (should be passed from docker-build-upload)"
    required: true
  argoapp:
    description: "ArgoCD application name"
    required: false
    default: ""
  image_name:
    description: "Image name in kustomization.yaml (defaults to repo value)"
    required: false
    default: ""
  timeout:
    description: "Timeout for app stabilization (e.g., 120)"
    required: false
    default: "120"
  git_user_name:
    description: "Git user name for commit"
    required: false
    default: "github-actions[bot]"
  git_user_email:
    description: "Git user email for commit"
    required: false
    default: "github-actions[bot]@truestory.work"

outputs:
  status:
    description: "Deployment status (success or failed)"
    value: ${{ steps.result.outputs.status }}

runs:
  using: "composite"
  steps:

    - name: Set vars
      shell: bash
      run: |
        REPO_FULL="${{ inputs.registry }}/${{ inputs.repo || github.event.repository.name }}"
        ARGOAPP="${{ inputs.argoapp || github.event.repository.name }}"
        IMAGE_FULL="${REPO_FULL}:${{ inputs.tag }}"
        KUSTOMIZE="${ARGOAPP}/kustomization.yaml"
        
        echo "REPO_FULL="${{ inputs.registry }}/${{ inputs.repo || github.event.repository.name }}"" >> $GITHUB_ENV
        echo "IMAGE_FULL=${IMAGE_FULL}" >> $GITHUB_ENV
        echo "ARGOAPP=${ARGOAPP}" >> $GITHUB_ENV
        echo "KUSTOMIZE=${KUSTOMIZE}" >> $GITHUB_ENV

    - uses: actions/checkout@v4
      with:
        ref: refs/heads/main
        show-progress: false
        repository: theverytruestory/deployments
        ssh-key: ~/.ssh/id_ed25519
        sparse-checkout: k0s/apps
        path: deployments
        fetch-depth: 1

    - name: ${{ env.ARGOAPP }} status before deploy
      working-directory: deployments/k0s/apps
      shell: bash
      run: |
        echo '<details><summary> before</summary>' >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        argocd --core app get ${{ env.ARGOAPP }} -o tree | tee -a $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo '</details>' >> $GITHUB_STEP_SUMMARY


    - name: Set ${{ env.IMAGE_FULL }} to ${{ env.ARGOAPP }} and wait for healthy
      working-directory: deployments/k0s/apps
      shell: bash
      run: |
        echo '<details><summary>set and wait</summary>' >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        argocd --core app set ${{ env.ARGOAPP }} --kustomize-image="${{ env.IMAGE_FULL }}"  | tee -a $GITHUB_STEP_SUMMARY
        argocd --core app wait ${{ env.ARGOAPP }} --timeout ${{ inputs.timeout }}  --health -o tree | tee -a $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo '</details>' >> $GITHUB_STEP_SUMMARY


    - name: git ${{ env.IMAGE_FULL }} to ${{ env.ARGOAPP }}
      working-directory: deployments/k0s/apps
      shell: bash
      run: |
        
        yq eval '(.images[] | select(.name == "${{ env.REPO_FULL }}") | .newTag) = "${{ inputs.tag }}"'  -i ${{ env.KUSTOMIZE }}

        echo '```diff' >> $GITHUB_STEP_SUMMARY
        git diff -U0 --color=always | tee -a $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY

        git add ${{ env.KUSTOMIZE }}        
        git config --global user.name "${{ inputs.git_user_name }}"
        git config --global user.email "${{ inputs.git_user_email }}"

        msg='${{ github.actor }}@${{ github.event.repository.name }}#${{ github.run_number }}: ${{ github.event.head_commit.message }}'
        git commit -m "${msg}"  || echo -e "git-commit exitcode $? No changes in manifest"
        git push origin main

    - name: Rollback on failure
      if: failure()
      shell: bash
      run: |
        echo '### failure! uset image' >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        argocd --core app unset ${{ env.ARGOAPP }} --kustomize-image="${{ env.IMAGE_FULL }}"
        argocd --core app wait ${{ env.ARGOAPP }} --timeout ${{ inputs.timeout }}  --health -o tree | tee -a $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY

